export const ArrayToJson = (targetArray) => {
  const parsedArray = [];
  targetArray.forEach((elex) => { 
    parsedArray.push(JSON.parse(elex))
  })
  return parsedArray;
}

export const ArrayToJson2 = (targetArray) => {
  const parsedArray = [];
  targetArray.forEach((elex) => { 
    parsedArray.push(JSON.parse(elex))
  })
  return parsedArray;
}